<%= form_for(@monthlyicon) do |f| %>
  <% if @monthlyicon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monthlyicon.errors.count, "error") %> prohibited this monthlyicon from being saved:</h2>

      <ul>
      <% @monthlyicon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :monthly_expenses %><br>
    <%= f.text_field :monthly_expenses %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
